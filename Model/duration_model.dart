class MyDurations {
/*durations=======================================*/
  static const Duration timeSheedAnim = Duration(milliseconds: 200);
  static const Duration timePlayerAnim = Duration(milliseconds: 150);
    static const Duration timeArrowAnim = Duration(milliseconds: 500);
    
    static const Duration timeSeekTo = Duration(seconds: 10);
/*================================================*/
}
